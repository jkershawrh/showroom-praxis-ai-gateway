= Conclusion

You built and assessed a governed AI model-access boundary rather than another provider-specific client integration.

== What you accomplished

* Verified and recovered Praxis AI Gateway on an AMD64 OpenShift worker
* Kept the model credential in an OpenShift Secret
* Proved that clients depend only on the Praxis endpoint
* Correlated a learner request with its response and trace identifier without claiming unavailable hop-by-hop telemetry
* Exercised explicit failure behavior and recovery
* Evaluated tenant, data, retention, and industry-policy decisions
* Preserved MaaS as a replaceable lab model source
* Created a sanitized, reusable evidence and readiness bundle

== Preserve your work

List the learner-created archive and use the terminal file browser or download function to save it before the RHDP environment expires.

[source,bash,role="execute",subs="attributes+"]
----
ls -lh ~/praxis-lab/artifacts/praxis-evidence-*.tar.gz
----

Stop the local port-forward. RHDP owns project and credential cleanup when the service is deleted; do not manually delete the assigned project unless an instructor directs you to do so.

[source,bash,role="execute",subs="attributes+"]
----
pkill -f 'port-forward service/praxis-ai' || true
----

== Next steps

* Review the https://github.com/jkershawrh/praxis-lab[Praxis quickstart source and evidence].
* Explore the https://github.com/praxis-proxy/ai[Praxis upstream project].
* Plan the replaceable enterprise backend with https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/[Red Hat OpenShift AI documentation].
* Add a tested OAuth or OpenID Connect boundary before exposing a real model through a public route.
* Validate Track 2 and Track 3 policies against your organization's threat model and retention requirements.

== Final perspective

An AI gateway is useful because it concentrates change: clients retain a stable contract while platform teams evolve model providers, routing, evidence, and controls. This lab proves deployment recovery, live polyglot access, credential ownership, controlled failure, and OpenShift isolation. It deliberately labels telemetry and policy capabilities that are not implemented by this pinned experience instead of turning future work into a demo claim.

Powered by Red Hat OpenShift running on {intel_platform} processors.
